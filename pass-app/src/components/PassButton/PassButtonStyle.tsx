import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width:'100%',
    backgroundColor: '#fff',
    fontSize:20,
    height:60,
    padding:10,
    borderRadius:10,
    borderWidth:0,
    textAlign:'center',
    color:'black'
  }
});