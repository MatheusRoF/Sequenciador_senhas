import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    input:{
        width:'100%',
        backgroundColor: '#f2f',
        fontSize:20,
        height:60,
        padding:10,
        borderRadius:10,
        borderWidth:1,
        textAlign:'center',
        color:'black'
      }
  });